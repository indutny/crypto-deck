'use strict';

function Poker() {
  this.src = null;
}
module.exports = Poker;

Poker.prototype.draw = function draw(player) {
};

Poker.prototype.onDraw = function onDraw(player, card) {
};

Poker.prototype.open = function open(player) {
};

Poker.prototype.onOpen = function onOpen(player, card) {
};
